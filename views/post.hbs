<!DOCTYPE html>
<html lang="en">
    <head>
        {{> head }}
    <body>
        {{> navbar }}

        <h1>blog post</h1>
        <p>{{ post.date }}</p>
        <p>{{ post.category }}</p>

        <article>
            {{{ post.content }}}
        </article>

        <!--<p>{{ author.name }} - {{ author.description }}</p>
        <img src="{{ author.image }}">-->

        {{> footer }}
    </body>
</html>
